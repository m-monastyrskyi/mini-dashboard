// Normally I would use something like process.env.REACT_APP_API_URL
// but for this project I'm using a hardcoded url
export const API_URL = 'https://jsonplaceholder.typicode.com';

export const ENDPOINTS = {
	USERS: 'users',
	COMMENTS: 'comments',
	POSTS: 'posts',
};
